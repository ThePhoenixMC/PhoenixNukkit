package com.lss233.phoenix.nukkit.utils.nukkit.item;

import com.lss233.phoenix.item.inventory.ItemType;

public class ItemStackHelper {

    /**
     * Gets the id of the ItemType in Bedrock Edition.
     * If unsupported, return -1.
     * @param type The ItemType.
     * @return The id if supported. Or -1 if unsupported.
     */
    public static int getItemId(ItemType type) {
        return -1; // TODO Implements later.
    }

    /**
     * Gets the ItemType of the item by id in Bedrock Edition.
     * If unsupported, return null.
     * @param id The id of the item.
     * @return The item type if supported. Or null if unsupported.
     */
    public static ItemType getItemType(int id) {
        return null; // TODO Implements later.
    }
}
